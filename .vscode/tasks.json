{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build",
            "type": "cargo",
            "command": "build",
            "options": {
                "cwd": "${workspaceFolder}/src-tauri/"
            },
            "problemMatcher": [
                "$rustc"
            ]
        }
    ]
}